name: PR Analysis

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate diff
        id: diff
        run: |
          git diff origin/${{ github.base_ref }}...HEAD > pr.diff

      - name: Invoke PR Risk Generator API
        id: analysis
        run: |
          DIFF_JSON=$(python3 -c 'import json; print(json.dumps(open("pr.diff").read()))')
          echo "Calling API: ${{ secrets.API_GATEWAY_URL }}/analyze"
          RESPONSE=$(curl -s -X POST \
            "${{ secrets.API_GATEWAY_URL }}/analyze" \
            -H "Content-Type: application/json" \
            -d "{
              \"diff\": $DIFF_JSON,
              \"pr_title\": \"${{ github.event.pull_request.title }}\",
              \"repo\": \"${{ github.repository }}\",
              \"pr_number\": ${{ github.event.pull_request.number }}
            }")
          echo "API Response: $RESPONSE"
          echo "response=$RESPONSE" >> $GITHUB_OUTPUT

      - name: Post analysis as PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const analysis = JSON.parse(`${{ steps.analysis.outputs.response }}`);
            const body = [
              `## PR Analysis`,
              `**Tag:** \`${analysis.modification_tag}\``,
              `### Summary`,
              analysis.summary,
              `### Risk Notes`,
              analysis.risk_notes.map(r => `- ${r}`).join('\n'),
              `### Test Checklist`,
              analysis.test_checklist.map(t => `- [ ] ${t}`).join('\n'),
            ].join('\n\n');
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });
